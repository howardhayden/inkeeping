# Requirements traceability matrix

## How to read this matrix

This matrix links product/security requirements to implementation, evidence artifacts, and validation responsibilities. It distinguishes four evidence classes:

- **A — automated:** an executable test asserts the behavior. A test file's existence does not mean the current candidate passed. [`VALIDATION_REPORT.md`](VALIDATION_REPORT.md) is immutable historical evidence for its identified 2026-08-21 working tree, not proof for a later candidate; consult the candidate-specific execution record.
- **I — implementation inspection:** the behavior is directly visible in source/configuration, but automated evidence is absent or incomplete.
- **M — manual:** a human interaction, visual, workflow, or document review is required.
- **E — external:** evidence comes from the live host, institution, vendor/product, account, or policy system outside this repository.

Status terms:

- **Implemented:** source contains the control and named automated evidence exists.
- **Implemented; inspection only:** source contains the control but a dedicated executable assertion is not identified.
- **Design target; manual evidence pending:** implementation support exists, but the quality claim cannot be made without manual results.
- **External acceptance required:** repository code cannot establish the requirement.

## Import and interchange

| ID | Requirement | Implementation | Evidence | Class | Status |
| --- | --- | --- | --- | --- | --- |
| IMP-001 | Reject empty, extension/MIME-inconsistent, invalid-UTF-8, control-character, and over-ceiling catalog files before admission. Foreign ceiling is 5 MiB. | [`reviewImport`](../app/lab-core.ts), `MAX_FILE_BYTES` | Catalog MIME/UTF-8/format cases in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented |
| IMP-002 | Permit up to 32 MiB only for a named `.in-keeping.json` file whose content is a strictly versioned catalog packet; do not let a disguised foreign JSON file use that ceiling. | `MAX_NATIVE_PACKET_BYTES`, `isCatalogPacket`, `parseJsonPacket` in [`app/lab-core.ts`](../app/lab-core.ts) | “versioned native packets remain self-importable above the foreign-file ceiling” in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented |
| IMP-003 | Bound JSON depth, strings, arrays, object width, and keys; reject prototype-related keys and reconstruct exact DTOs. | `inspectJson`, `exactKeys`, packet/stored validators in [`app/lab-core.ts`](../app/lab-core.ts); archive/backup counterparts | Prototype/future/unknown/shape tests in catalog, archive, and backup suites | A | Implemented |
| JSON-001 | Before semantic parsing, reject duplicate decoded JSON member names and unpaired Unicode surrogates in catalog/archive JSON, workspace backups, continuity receipts, and versioned tabular JSON cells. | [`app/json-safety.ts`](../app/json-safety.ts) and all operator-supplied raw/cell JSON entry points | [`tests/json-safety.test.mjs`](../tests/json-safety.test.mjs) plus catalog/archive/backup/receipt integration attacks | A | Implemented |
| JSONLD-001 | Accept only exact supported Schema.org context strings; reject nested/graph context overrides; separate public `@id` links from private IN KEEPING identity; require exact nonempty identifier value/scheme carriers. | JSON-LD dispatch and parsing in [`app/lab-core.ts`](../app/lab-core.ts) | Context, graph, identity-wrapper, and value/scheme cases in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented; semantic meaning and resolution remain external |
| CSL-001 | Keep CSL/JSON-LD dispatch explicit and reject case/NFKC lookalikes or wrong types for CSL identity keys. | JSON dispatch and CSL parser in [`app/lab-core.ts`](../app/lab-core.ts) | Extension/dispatch, alias, and type cases in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented |
| XML-001 | Scan XML before DOM construction and reject DTD/entity/PI content, malformed structure, depth >256, elements >100,000, counted nodes+attributes >100,000, attributes/element >64, and overlong tokens/text. | [`app/xml-safety.ts`](../app/xml-safety.ts) | Exact boundary and hostile declaration tests in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented |
| XML-002 | Reject foreign XML elements and applied foreign attributes; reject same-namespace elements/attributes outside the accepted format profile instead of ignoring them. | `assertXmlElementNamespaces` plus MARCXML/MODS/DC parsers in [`app/lab-core.ts`](../app/lab-core.ts) and EAD structure validation in [`app/archival-schemas.ts`](../app/archival-schemas.ts) | Foreign/same-namespace/hidden/attribute cases in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) and [`tests/archival-schemas.test.mjs`](../tests/archival-schemas.test.mjs) | A | Implemented |
| XML-003 | Allow an unused `xmlns` declaration because it contributes no record content; reject the document if that namespace is applied to a foreign element or attribute. | Namespace-declaration exemption in [`assertXmlElementNamespaces`](../app/xml-safety.ts) and format attribute checks | Positive unused-declaration fixture plus applied foreign element/attribute rejection in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented |
| CARD-001 | Reject canonical arrays above their maxima; never silently truncate oversized creator, contributor, identifier, link, or metadata arrays. | `importedRecord`, canonical validators, format parsers in [`app/lab-core.ts`](../app/lab-core.ts) | “every exchange format…array maxima,” “oversized record arrays…rejected,” and CSL/JSON-LD array tests | A | Implemented |
| RIS-001 | Parse every RIS line; require first/unique nonempty `TY`, at most one singular `ID`, terminal `ER  -`, no data after termination, and at most one blank separator; retain no more than 1,024 evidence elements including `ER`. | `sourceLines` and `parseRis` in [`app/lab-core.ts`](../app/lab-core.ts) | Malformed, duplicate ID/type, unterminated, nonterminal, LF/CRLF, separator, 1,024/overflow cases | A | Implemented |
| MARC-001 | Parse every MARC mnemonic/MARCXML field; require valid leader/lines/indicators/subfields, singular `001`/`003`, and per-field `020`/`022`/`024` identity cardinality while preserving distinct repeatable fields. | MARC parsers in [`app/lab-core.ts`](../app/lab-core.ts) | Mnemonic/XML singular-control, repeatable-field, malformed, escaping, and 1,024-bound cases | A | Implemented |
| BIB-001 | Use a bounded BibTeX parser with nested braces; accept only braced/quoted/numeric values; reject directives, macros, concatenation, duplicate fields, and malformed delimiters; terminate percent comments on CR/LF/CRLF; allow 1,022 fields/1,024 evidence elements. | `parseBibtexSource` in [`app/lab-core.ts`](../app/lab-core.ts) | Nested/quoted/directive/macro/concatenation/comment-line-ending/duplicate/64-depth/1,022–1,023 cases | A | Implemented |
| TAB-001 | Validate delimited headers, row widths, quoting, columns, and booleans; preserve versioned lists/TSV escapes; neutralize formula-leading export cells. | Delimited parsers in [`app/lab-core.ts`](../app/lab-core.ts), serializers in [`app/record-formats.ts`](../app/record-formats.ts), archive CSV code | Delimited rejection, lossless CSV/TSV, serializer injection, archival CSV round-trip tests | A | Implemented |
| URL-001 | Validate public credential-free HTTPS syntax without a network request; reject local/private/reserved literals, local names, trailing dots, and secret-like query keys. | [`app/public-url.ts`](../app/public-url.ts) | [`tests/public-url.test.mjs`](../tests/public-url.test.mjs) and configuration/import tests | A | Implemented |
| APPLY-001 | Review must not mutate state; Apply must accept only the exact unchanged successful review object and revalidate provenance, exact record/schema shape, findings, destination IDs/capacity, and complete-set invariants. | Bound review capabilities and Apply functions in [`app/lab-core.ts`](../app/lab-core.ts) and [`app/archival-schemas.ts`](../app/archival-schemas.ts) | Coherent catalog/archival review-substitution, conflict, and revision tests | A | Implemented |
| EVID-001 | Consumption must require an exact no-default disposition and preserve one linked applied/not-applied result. Source kind, decision, outcome, reason, and applied revision ID/state digest must agree. Active admission remains an output barrier after withdrawal; removed scopes leave reportable history without a global latch. Unattributed archive/service content also blocks. | [`app/evidence-authority.ts`](../app/evidence-authority.ts), Apply/Open integration, active-reachability output/report gates | Evidence application contradiction/nonexistent-target, conflict/capacity, manual-entry, backup rejection, and withdrawal-laundering attacks across evidence/core/output/report suites | A + E | Implemented local boundary; external authenticity/corroboration required |
| ID-001 | Reject unsafe supplied catalog primary IDs; when a primary ID is absent, derive a stable local ID from the selected-file digest and ordinal. Normalize supported DOI label prefixes before duplicate comparison. | Catalog parsers, `importedRecord`, identifier normalization, and review findings in [`app/lab-core.ts`](../app/lab-core.ts) | Unsafe/absent primary-ID and DOI-label duplicate tests in [`tests/lab-core.test.mjs`](../tests/lab-core.test.mjs) | A | Implemented; semantic identity still requires authoritative reconciliation |
| ID-002 | Require archival reference codes to be unique across distinct records in one validated archive set. | `validateArchiveSet` in [`app/archival-schemas.ts`](../app/archival-schemas.ts) | Distinct-record duplicate-reference-code test in [`tests/archival-schemas.test.mjs`](../tests/archival-schemas.test.mjs) | A | Implemented; institutional authority control remains external |
| ID-003 | Reject contradictory format-defined singular identities in RIS, MARC/MARCXML, MODS, Dublin Core, EAD, JSON-LD, and CSL while preserving standards-repeatable identifier evidence within bounds. | Format parsers in [`app/lab-core.ts`](../app/lab-core.ts) and [`app/archival-schemas.ts`](../app/archival-schemas.ts) | Cross-format singular/repeatable identity regressions | A | Implemented; real-world entity resolution remains external |
| ARCH-001 | Archival import/export must support the declared bounded EAD/CSV/schema profiles and reject unsupported structures/hierarchy/depth rather than infer silently. EAD must reject populated custom IDs, altered populated-core kind/cardinality/mapping, and mismatched scalar/array shapes rather than join or omit values. | [`app/archival-schemas.ts`](../app/archival-schemas.ts) | EAD profile, custom-value, editable-core, shape, and round-trip cases in [`tests/archival-schemas.test.mjs`](../tests/archival-schemas.test.mjs) | A | Implemented; current-candidate execution is recorded separately |
| INT-001 | Validate representative exchange output in institution-supported cataloging, archival, spreadsheet, discovery, and preservation products before institutional reliance. | Application supplies documented export profiles and native lossless packages | Vendor/product/version fixture protocol in [`TESTING.md`](TESTING.md) | E + M | External acceptance required |

## Evidence presentation and interface scale

| ID | Requirement | Implementation | Evidence | Class | Status |
| --- | --- | --- | --- | --- | --- |
| UI-REC-001 | Present catalog evidence as two complete, separately labeled blocks: Original input and New output, with source element names, values, and accessible definitions. | Record comparison components in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | Interface source contracts; Technical Report record tests in [`tests/report-documents.test.mjs`](../tests/report-documents.test.mjs) | A + M | Implemented; manual comprehension review required |
| UI-PAGE-001 | Use one shared pagination contract with no more than 100 rows per list page; clamp invalid pages and keep a selected record on its owning page. | [`app/list-pagination.ts`](../app/list-pagination.ts), list integrations in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | [`tests/list-pagination.test.mjs`](../tests/list-pagination.test.mjs), interface integration assertions | A | Implemented |
| UI-PAGE-002 | Bound the archival parent chooser rather than rendering the full possible hierarchy as options. | Search/filter plus `slice(0, 100)` in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | Interface contract test | A + M | Implemented; interaction review required |
| UI-DRAFT-001 | Prevent silent loss of in-progress editor/note/workspace-name drafts during navigation, selection, paging, workspace actions, and unload. | Draft registry/guards in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | Interface source contracts | A(source) + M | Implemented; browser interaction review required |
| UI-STATUS-001 | Announce import, backup, storage recovery, and global operation results with status/alert semantics and identify action targets. | ARIA live/status/alert regions and accessible names in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | Interface source contracts | A(source) + M | Implemented; AT validation pending |
| UI-FRESH-001 | For every authoritative UI artifact, require a named clean non-recovery session and exact current independent receipt; reopen/validate the saved generation with that receipt, compare token/state/audit/revision/anchor/evidence, render the reopened snapshot, and reopen/recheck the same receipt and fingerprint before activation. Save/rename/reload clear proof. | [`app/output-freshness.ts`](../app/output-freshness.ts) and integrations in [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | [`tests/output-freshness.test.mjs`](../tests/output-freshness.test.mjs), anchored storage tests, and interface contracts | A + M | Implemented; receipt independence, post-final-read currency, and browser activation require manual/external evidence |

## Persistence, backup, and integrity

| ID | Requirement | Implementation | Evidence | Class | Status |
| --- | --- | --- | --- | --- | --- |
| STO-001 | Begin with an in-memory working copy; persist only after explicit named-workspace create/save/open behavior. Do not use fixed slots. | Workspace manager and [`app/lab-storage.ts`](../app/lab-storage.ts) | Named workspace interface and storage tests | A + M | Implemented |
| STO-002 | Validate/serialize a workspace within 25 MiB and use one IndexedDB transaction with optimistic token/anchor recheck for manifest/generation/anchor save. | `writeWorkspace`, bounded serialization, `completeTransaction` in [`app/lab-storage.ts`](../app/lab-storage.ts) | Atomic save, token, anchor-race, quota/error tests in [`tests/lab-storage.test.mjs`](../tests/lab-storage.test.mjs) | A | Implemented |
| STO-003 | Retain active and one prior generation under a manifest that binds both payload digests. | `LocalWorkspaceManifest`, `writeWorkspace`, `openLocalWorkspace` | “saves bind both retained generations…” and legacy-upgrade cases | A | Implemented |
| STO-004 | If active generation and manifest digest disagree, stop without fallback. Never trust a prior generation that lacks a manifest-bound digest. | `verifyGeneration`, `openLocalWorkspace` in [`app/lab-storage.ts`](../app/lab-storage.ts) | Active disagreement, substituted fallback, legacy-unbound fallback tests | A | Implemented |
| STO-005 | Fail closed above 100 inspected manifests or 256 generation keys and surface invalid/orphan/unreferenced/missing states as quarantine rather than silently hiding them. | `inspectLocalWorkspaceStorage`, bounded cursor readers | Exact 100/101 and 256/257 tests; quarantine listing test | A | Implemented |
| STO-006 | Inspect a quarantined candidate by recomputing digest and validating complete snapshot/audit; reconstruct only into a new UUID/name and do not modify source bytes. | `inspectLocalWorkspaceRecoveryCandidate`, `reconstructLocalWorkspaceFromQuarantine` | Invalid/orphan/corrupt candidate and source-preservation tests | A | Implemented |
| STO-007 | Before a normal or recovery save opens its write transaction, re-read and fully verify the manifest-bound base generation; recheck the token in the transaction; verify any prior generation before deleting it; reject healthy-active recovery substitution, changed fallback input, unsafe generation increment, or manifest/display metadata mismatch. Missing-active storage must not surface unbound manifest display metadata; fallback labels and counts derive from the verified fallback. | `requireVerifiedSaveBase`, `activeManifestMetadataMatches`, `openedGenerationManifest`, and `writeWorkspace` in [`app/lab-storage.ts`](../app/lab-storage.ts) | Missing/corrupt active and prior, substituted recovery input, healthy-active recovery, legacy-unbound fallback, metadata/display substitution, and generation-boundary tests | A | Implemented |
| AUD-003 | Create a local continuity baseline only after explicit acknowledgment; bind the exact saved payload/audit/generation/workspace/lineage; advance an existing anchor only through a consecutive exact full-ledger prefix; reject successor-looking reset ledgers; require an exact current unsigned receipt for ordinary output. | [`app/continuity-anchor.ts`](../app/continuity-anchor.ts), atomic-bundle receipt/open integration in [`app/lab-storage.ts`](../app/lab-storage.ts) | [`tests/continuity-anchor.test.mjs`](../tests/continuity-anchor.test.mjs) and anchored storage regeneration/co-rewrite/stale-receipt cases | A + E | Implemented local continuity; multi-generation proof, clean-device continuity, authenticity, independent custody, signing, and trusted time external |
| BAK-001 | Workspace backup v2 must identify itself with `protection: plaintext-json-not-encrypted`; missing/misstated marker rejects. | [`app/workspace-backups.ts`](../app/workspace-backups.ts) | Marker tests in [`tests/workspace-backups.test.mjs`](../tests/workspace-backups.test.mjs) | A | Implemented |
| BAK-002 | Review backup extension/MIME/size/UTF-8, raw JSON uniqueness/Unicode, exact envelope/version/time, keys/shape, payload digest, and full workspace state; Open requires the exact unchanged review plus explicit admit-unverified disposition and appends an outer backup evidence record. | `reviewWorkspaceBackup`, `verifyWorkspaceBackupReviewBinding`, UI consumption, and `validateWorkspaceSnapshot` | Backup JSON/tamper/boundary, clone/substitution, disposition, and `null`/`NaN` collision tests | A | Implemented; backup/nested claims remain unauthenticated |
| BAK-004 | State that a workspace backup excludes IndexedDB manifest/generations/tokens, local continuity anchor, and downloaded receipts; clean-device/origin restore creates an unanchored new lineage. | Backup/UI/operations/governance documentation | Source inspection plus clean-device drill | I + M | Implemented disclosure; manual recovery evidence required |
| BAK-003 | Disclose that IndexedDB and downloads are plaintext and outside application encryption/access/retention controls. | UI copy, backup marker, reports, [`SECURITY.md`](SECURITY.md), [`PRIVACY_AND_DATA_GOVERNANCE.md`](PRIVACY_AND_DATA_GOVERNANCE.md) | Envelope assertion and interface source contracts; governance review | A(partial) + M | Implemented; institutional handling approval required |
| AUD-001 | Validate exact event positions, previous-hash links, event hashes, latest current-state digest, revision digests, one forward retained revision lineage, and the complete saved-snapshot shape. Validate the full predecessor before audit rollover. | `validateWorkspaceSnapshot`, `verifyAudit`, rollover guards, and digest functions in [`app/lab-core.ts`](../app/lab-core.ts) | Tamper, rollover, linear-lineage, storage, backup, and interface verification tests | A | Implemented |
| AUD-002 | Do not claim audit hashes prove actor identity, authorization, custody, trusted time, nonrepudiation, or all tail truncation. | Technical Report safeguards and governing documentation | Report text assertions plus manual review | A(text) + M | Implemented limitation; governance use restriction required |

## Privacy, reports, deployment, and accessibility

| ID | Requirement | Implementation | Evidence | Class | Status |
| --- | --- | --- | --- | --- | --- |
| PRI-001 | Application code must have no analytics, telemetry, cookie, remote-font, background-upload, imported-URL-fetch, or workspace API path. | Static browser code, self-hosted fonts, CSP `connect-src 'none'` | Production/report contracts plus source scan; live Network capture required | A(partial) + I + E | Implemented in inspected source; live verification required |
| PRI-002 | Distinguish application workspace behavior from ordinary Cloudflare/DNS/TLS/HTTP metadata and account-level logging. | Architecture and privacy documentation; Wrangler observability disabled | Source configuration; Cloudflare account review | I + E | External acceptance required |
| PRI-003 | Treat application caps as operational bounds, not retention authority; deletion must not be described as deleting exports/platform copies. | UI/report/docs and local deletion behavior | Source inspection plus institutional records schedule | I + E | External acceptance required |
| REP-001 | Technical Report and Public Notice must be static post-Jupyter HTML with embedded Jost, no script/remote resource, restrictive document CSP, semantic linear diagrams, and bounded generation. | [`app/report-documents.ts`](../app/report-documents.ts) | [`tests/report-documents.test.mjs`](../tests/report-documents.test.mjs) | A + M | Implemented; browser/AT review required |
| REP-002 | Public Notice receives only fixed nonsynthetic open-incident categories; blocks samples, unsupported closures, and admitted-unverified evidence; and in the UI renders the exact reopened saved snapshot under the authoritative lease with a final post-construction recheck. | Public projection in [`app/report-documents.ts`](../app/report-documents.ts), lease/UI integration | Isolation, sample, closure, evidence, empty-scope, lease-race, and interface tests | A + M | Implemented; publication approval and later currency external |
| REP-003 | Report generators derive snapshot integrity rather than trust a caller; invalid Technical Reports say mismatch/action required and invalid Public Notices reject. The UI supplies Technical Report freshness/continuity from live inspection, but the direct generator's context is not independent proof. | Report renderers and diagnostic lease integration | Tampered snapshot, contradictory status, evidence/continuity, empty scope, timestamp, and diagnostic freshness tests | A + M | Implemented diagnostic boundary; semantic and institutional approval external |
| INC-001 | The transition to `resolved` must include an assigned owner role, closure criterion, and contemporaneous note in the same mutation. A later owner/criterion change while remaining resolved also requires a contemporaneous note. Incident-bound documents must require an explicit incident ID; truthful outputs must reject resolved incidents missing owner, note, or criterion. | `updateIncident`, `makeOperationalDocument`, report guards, and incident update/report UI in [`app/lab-core.ts`](../app/lab-core.ts), [`app/report-documents.ts`](../app/report-documents.ts), and [`app/continuity-lab.tsx`](../app/continuity-lab.tsx) | Closure-evidence/criterion revision, assigned-owner, explicit-target, legacy-closure, and interface form tests | A + M | Implemented; truth and authorization of closure evidence require external review |
| REP-004 | Catalog, archive, service, operational, and public outputs fail closed for invalid/unchecked, sample, unnamed, dirty, recovery, stale, unanchored/failed-continuity, admitted/unattributed-evidence, or unresolved finding state. Informational notices remain visible. Empty catalog rejects. Technical Report and explicit recovery backup remain diagnostic. | Shared UI gate, [`app/output-freshness.ts`](../app/output-freshness.ts), formatter preconditions | Interface, freshness, formatter, report, sample, finding, evidence, and continuity tests | A + M | Implemented; direct transformers, activation, destination review, and post-check currency remain external |
| DEP-001 | Production topology must be static assets with no application Worker entry point or D1/R2/KV/Durable Object binding and disabled configured observability. | [`wrangler.jsonc`](../wrangler.jsonc) | [`tests/production-contracts.test.mjs`](../tests/production-contracts.test.mjs), artifact validator, Wrangler dry run | A + E | Implemented in source; release/live evidence required |
| DEP-002 | Serve the defined CSP and security headers from the canonical HTTPS origin; canonical configuration must be scheme/host-only HTTPS. | [`security-headers.ts`](../security-headers.ts), [`public/_headers`](../public/_headers), site-origin validator | Production/rendered tests; live HTTP capture | A + E | Implemented in source; live evidence required |
| DEP-003 | Run lint, all TypeScript surfaces, full build/tests, high-severity dependency audit, artifact validation, and strict Wrangler dry run before release. | `release:check` in [`package.json`](../package.json) | Candidate-specific CI/release log; [`VALIDATION_REPORT.md`](VALIDATION_REPORT.md) is a historical example only | A(execution) | Required for each immutable release candidate |
| A11Y-001 | Provide language, skip/main landmarks, heading hierarchy, native labeled controls, visible focus, text status/error, and action-specific names. | [`app/continuity-lab.tsx`](../app/continuity-lab.tsx), [`app/globals.css`](../app/globals.css), report renderer | Interface/report source contracts plus manual protocol in [`ACCESSIBILITY.md`](ACCESSIBILITY.md) | A(source) + M | Design target; manual evidence pending |
| A11Y-002 | Keep required content within viewport-owned scroll regions and support reflow/zoom without an arbitrary fixed desktop canvas. | `100dvh` shell, scroll owners, responsive CSS | Interface CSS contracts plus manual reflow/zoom matrix | A(source) + M | Design target; manual evidence pending |
| A11Y-003 | Validate safety-critical flows with institution-supported keyboard, browser, screen reader, zoom, contrast, forced-colors, and reduced-motion configurations. | Manual protocol in [`ACCESSIBILITY.md`](ACCESSIBILITY.md) | Completed evaluation record | M + E | External acceptance required; no conformance claim yet |

## Evidence gaps and required actions

| Gap | Required action | Release effect |
| --- | --- | --- |
| Focus/keyboard/draft/status contracts are primarily source-pattern tests. | Execute and record browser interaction and assistive-technology protocols. | Blocks accessibility conformance or production accessibility acceptance. |
| Source/configuration cannot prove live headers or absence of unexpected platform/network behavior. | Capture canonical-origin response headers and browser network activity after deployment. | Blocks production acceptance. |
| Repository tests cannot establish vendor-profile interoperability. | Import/export representative institutional fixtures with named product versions/configurations. | Blocks workflow reliance for each unvalidated exchange path. |
| Automated lease tests cannot prove browser user-activation, popup/download-manager behavior, or atomic currentness after the final IndexedDB read. | Exercise delayed/suppressed cross-tab notifications and changes before click/during large construction on supported browsers; confirm zero activation on failure and inspect the resulting file. | Blocks browser-level freshness acceptance. |
| Local evidence claims and unsigned continuity receipts cannot establish source truth, custody, identity, trusted time, or independent storage. | Use the institution's governed source reconciliation, signing/timestamp, custody, and approval systems; retain external evidence. | Blocks authoritative reliance on imported/restored data and forensic/nonrepudiation claims. |
| Wrangler observability setting cannot establish all Cloudflare account logging/analytics behavior. | Complete platform privacy/security review and retain account evidence. | Blocks institutional privacy approval. |
| The historical validation report covers only its named 2026-08-21 modified working tree; later red-team changes invalidate affected results. | Run `npm run release:check` on the immutable release commit and create a new candidate-specific acceptance record without relabeling the historical 139-test result. | Blocks release. |

## Change-control rule

Every pull request that changes a requirement or its implementation must update the corresponding row. A row cannot move to “Implemented” solely because documentation was added. It requires code plus executable evidence, or an explicit inspection/manual/external classification where automation cannot establish the claim.
